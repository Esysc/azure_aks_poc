# Example Terraform variables
# Copy to terraform.tfvars and adjust as needed

resource_group_name = "rg-aks-poc"
location            = "westeurope"
cluster_name        = "aks-poc-cluster"
dns_prefix          = "akspoc"
node_count          = 1
vm_size             = "Standard_B2s"
